import { admitRuntimeAdminRequest } from "@/lib/admin-access-runtime";
import { adminFailure, adminSuccess } from "@/lib/admin-api";
import { AdminProductConflictError, AdminProductIdempotencyConflictError, AdminProductNotFoundError, AdminProductValidationError, getProduct, updateProduct } from "@/lib/admin-product-repository";

export const dynamic="force-dynamic";const MAX_BODY_BYTES=64*1024;
export async function GET(request:Request,{params}:{params:Promise<{id:string}>}){const requestId=crypto.randomUUID();const admission=await admitRuntimeAdminRequest(request);if(!admission.ok)return adminFailure(requestId,admission.status,admission.code,admission.message);const id=parseId((await params).id);if(id===null)return adminFailure(requestId,404,"NOT_FOUND","Không tìm thấy sản phẩm.");const product=await getProduct(id);return product?adminSuccess(requestId,product):adminFailure(requestId,404,"NOT_FOUND","Không tìm thấy sản phẩm.");}
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){const requestId=request.headers.get("x-request-id")?.trim()||crypto.randomUUID();const admission=await admitRuntimeAdminRequest(request);if(!admission.ok)return adminFailure(requestId,admission.status,admission.code,admission.message);if(!isJson(request.headers.get("content-type")))return adminFailure(requestId,415,"UNSUPPORTED_MEDIA","Content-Type không được hỗ trợ.");const id=parseId((await params).id);if(id===null)return adminFailure(requestId,404,"NOT_FOUND","Không tìm thấy sản phẩm.");try{return adminSuccess(requestId,await updateProduct(id,await readJson(request),admission.actor.subject,requestId));}catch(error){if(error instanceof PayloadTooLargeError)return adminFailure(requestId,413,"PAYLOAD_TOO_LARGE","Request quá lớn.");if(error instanceof AdminProductConflictError)return adminFailure(requestId,409,"UNIQUE_CONFLICT","Slug hoặc SKU sản phẩm đã tồn tại hoặc có xung đột.");if(error instanceof AdminProductIdempotencyConflictError)return adminFailure(requestId,409,"IDEMPOTENCY_CONFLICT","Request ID đã được sử dụng cho payload khác.");if(error instanceof AdminProductNotFoundError)return adminFailure(requestId,404,"NOT_FOUND","Không tìm thấy sản phẩm.");if(error instanceof AdminProductValidationError||error instanceof Error&&/required|invalid|must be|exceeds|slug|sku|category/i.test(error.message))return adminFailure(requestId,422,"VALIDATION_ERROR","Dữ liệu sản phẩm không hợp lệ.");return adminFailure(requestId,500,"INTERNAL_ERROR","Không thể hoàn tất thao tác quản trị.");}}
function parseId(v:string){const n=Number(v);return Number.isSafeInteger(n)&&n>0?n:null;}function isJson(v:string|null){return Boolean(v&&v.toLowerCase().split(";",1)[0].trim()==="application/json");}async function readJson(request:Request){const length=Number(request.headers.get("content-length"));if(Number.isFinite(length)&&length>MAX_BODY_BYTES)throw new PayloadTooLargeError();const bytes=await request.arrayBuffer();if(bytes.byteLength>MAX_BODY_BYTES)throw new PayloadTooLargeError();try{return JSON.parse(new TextDecoder().decode(bytes));}catch{throw new AdminProductValidationError("Malformed JSON request.");}}class PayloadTooLargeError extends Error{}
